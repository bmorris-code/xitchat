# Deprecated - using Vercel for all deployments
# This workflow is disabled (manual trigger only, does nothing)

name: Deprecated - Use Vercel

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
    - name: Notice
      run: echo "Use Vercel for deployment. This workflow is disabled."
