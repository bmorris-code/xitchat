# This workflow is deprecated - we use Vercel for deployment now
# Please use Vercel instead of GitHub Pages for APK hosting

name: Deprecated - Use Vercel

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
    - name: Deprecation Notice
      run: |
        echo "⚠️  This workflow is deprecated"
        echo "📱 Please use Vercel for deployment instead"
        echo "🔗 Deploy to Vercel: https://vercel.com"
        exit 1
